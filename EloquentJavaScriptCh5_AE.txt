/*Ch 5 Exercises
function flattening 	= line 
function Monther-Child
*/

function frustratedFlattening(a)
{
  var b = [];

  //a.reduce(
  a.forEach(function(c){
    b = b.concat(c);
  });
  
  return b;
}

function flattening(a)
{
 var b = [];

  a.reduce(function(c, d){//reduce function takes 4 parameters
    //https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/Reduce
      b = b.concat(d);
    }, []);//without ', []' only returns [4,5,6]
  return b;
}

--------------------------------
Mother-Child_age_difference
ancestry  = 'require(./path/to/ancestry.js)';
console.log(ancestry);

/*function average(array) {
  function plus(a, b) { return a + b; }
  return array.reduce(plus) / array.length;
}

var byName = {};
ancestry.forEach(function(person) {
  byName[person.name] = person;
});

function AgeDifference( mother, child )
{
  var ageDifference = child.born - mother.born;

 return ageDifference; 
}

function AverageAge(set)
{
  var ageDiffArray = [];
  
  forEach(person in set)
  { 
    var mother = byName(person.mother);
    ageDiffArray.push(AgeDifference(mother, person));
  }
  
  return average(ageDiffArray(ancestry));
}*/
